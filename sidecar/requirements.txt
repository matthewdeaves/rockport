# Sidecar dependencies — exact pins for reproducibility.
# Run `pip-compile --generate-hashes requirements.txt -o requirements.lock` to regenerate lock file.
psycopg2-binary==2.9.12
Pillow==12.2.0
httpx==0.28.1
