[extend]
useDefault = true

[[allowlists]]
description = "False positives — placeholder and intentionally-invalid keys in docs and tests"
stopwords = [
  "sk-your-key",
  "sk-invalid-key-12345",
  "Bearer sk-invalid-key-12345",
]
regexTarget = "match"
regexes = ['''sk-invalid-key-12345''']
